# doc-orchestrator
Config-first document processing orchestrator using local VLMs for doc classification and field extraction with strict validation, normalization, and verification to structured JSON outputs.
